# `just database-start` writes local values to `.env.local` automatically.
# Hosted builds also require a real Turnstile site key; never supply its secret key here.
VITE_TURNSTILE_SITE_KEY=
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=replace-with-a-publishable-key
